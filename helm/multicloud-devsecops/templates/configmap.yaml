apiVersion: v1

kind: ConfigMap

metadata:

  name: {{ include "multicloud-devsecops.fullname" . }}-config

  labels:

    {{- include "multicloud-devsecops.labels" . | nindent 4 }}

data:

  SPRING_PROFILES_ACTIVE: {{ .Values.config.springProfilesActive | quote }}

  SERVER_PORT: {{ .Values.config.serverPort | quote }}